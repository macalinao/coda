export DIRENV_WARN_TIMEOUT=20s

# nix-direnv's `use flake` automatically watches flake.nix and flake.lock,
# so the shell rebuilds when either changes.
use flake

# Replaces devenv's `dotenv.enable = true`.
dotenv_if_exists
