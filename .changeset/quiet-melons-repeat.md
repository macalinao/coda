---
---

Repo tooling only -- no change to any published package. `nix flake update` moved nixpkgs
from 2026-07-31 to 2026-09-08, and the root `oxlint`/`oxfmt` devDependency pins were synced
to the versions the devshell now provides.
