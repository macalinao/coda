/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
} from "@solana/kit";
import type { ResolvedInstructionAccount } from "@solana/program-client-core";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
} from "@solana/kit";
import { getAccountMetaFactory } from "@solana/program-client-core";
import { QUARRY_MINT_WRAPPER_PROGRAM_ADDRESS } from "../programs/index.js";

export const MINTER_UPDATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  11, 219, 68, 245, 38, 222, 221, 184,
]);

export function getMinterUpdateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MINTER_UPDATE_DISCRIMINATOR,
  );
}

export type MinterUpdateInstruction<
  TProgram extends string = typeof QUARRY_MINT_WRAPPER_PROGRAM_ADDRESS,
  TAccountMintWrapper extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMinter extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMintWrapper extends string
        ? WritableAccount<TAccountMintWrapper>
        : TAccountMintWrapper,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMinter extends string
        ? WritableAccount<TAccountMinter>
        : TAccountMinter,
      ...TRemainingAccounts,
    ]
  >;

export interface MinterUpdateInstructionData {
  discriminator: ReadonlyUint8Array;
  allowance: bigint;
}

export interface MinterUpdateInstructionDataArgs {
  allowance: number | bigint;
}

export function getMinterUpdateInstructionDataEncoder(): FixedSizeEncoder<MinterUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["allowance", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MINTER_UPDATE_DISCRIMINATOR }),
  );
}

export function getMinterUpdateInstructionDataDecoder(): FixedSizeDecoder<MinterUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["allowance", getU64Decoder()],
  ]);
}

export function getMinterUpdateInstructionDataCodec(): FixedSizeCodec<
  MinterUpdateInstructionDataArgs,
  MinterUpdateInstructionData
> {
  return combineCodec(
    getMinterUpdateInstructionDataEncoder(),
    getMinterUpdateInstructionDataDecoder(),
  );
}

export interface MinterUpdateInput<
  TAccountMintWrapper extends string = string,
  TAccountAdmin extends string = string,
  TAccountMinter extends string = string,
> {
  mintWrapper: Address<TAccountMintWrapper>;
  admin: TransactionSigner<TAccountAdmin>;
  minter: Address<TAccountMinter>;
  allowance: MinterUpdateInstructionDataArgs["allowance"];
}

export function getMinterUpdateInstruction<
  TAccountMintWrapper extends string,
  TAccountAdmin extends string,
  TAccountMinter extends string,
  TProgramAddress extends Address = typeof QUARRY_MINT_WRAPPER_PROGRAM_ADDRESS,
>(
  input: MinterUpdateInput<TAccountMintWrapper, TAccountAdmin, TAccountMinter>,
  config?: { programAddress?: TProgramAddress },
): MinterUpdateInstruction<
  TProgramAddress,
  TAccountMintWrapper,
  TAccountAdmin,
  TAccountMinter
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? QUARRY_MINT_WRAPPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    mintWrapper: { value: input.mintWrapper ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    minter: { value: input.minter ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("mintWrapper", accounts.mintWrapper),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("minter", accounts.minter),
    ],
    data: getMinterUpdateInstructionDataEncoder().encode(
      args as MinterUpdateInstructionDataArgs,
    ),
    programAddress,
  } as MinterUpdateInstruction<
    TProgramAddress,
    TAccountMintWrapper,
    TAccountAdmin,
    TAccountMinter
  >);
}

export interface ParsedMinterUpdateInstruction<
  TProgram extends string = typeof QUARRY_MINT_WRAPPER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    mintWrapper: TAccountMetas[0];
    admin: TAccountMetas[1];
    minter: TAccountMetas[2];
  };
  data: MinterUpdateInstructionData;
}

export function parseMinterUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMinterUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      mintWrapper: getNextAccount(),
      admin: getNextAccount(),
      minter: getNextAccount(),
    },
    data: getMinterUpdateInstructionDataDecoder().decode(instruction.data),
  };
}
