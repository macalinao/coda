/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  Codec,
  Decoder,
  Encoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlyAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
  WritableSignerAccount,
} from "@solana/kit";
import type { ResolvedInstructionAccount } from "@solana/program-client-core";
import type {
  CreateOrUpdateArgs,
  CreateOrUpdateArgsArgs,
} from "../types/index.js";
import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
} from "@solana/kit";
import { getAccountMetaFactory } from "@solana/program-client-core";
import { MPL_TOKEN_AUTH_RULES_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  getCreateOrUpdateArgsDecoder,
  getCreateOrUpdateArgsEncoder,
} from "../types/index.js";

export const CREATE_OR_UPDATE_DISCRIMINATOR = 0;

export function getCreateOrUpdateDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CREATE_OR_UPDATE_DISCRIMINATOR);
}

export type CreateOrUpdateInstruction<
  TProgram extends string = typeof MPL_TOKEN_AUTH_RULES_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta = string,
  TAccountRuleSetPda extends string | AccountMeta = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta = "11111111111111111111111111111111",
  TAccountBufferPda extends string | AccountMeta = string,
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountRuleSetPda extends string
        ? WritableAccount<TAccountRuleSetPda>
        : TAccountRuleSetPda,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountBufferPda extends string
        ? ReadonlyAccount<TAccountBufferPda>
        : TAccountBufferPda,
      ...TRemainingAccounts,
    ]
  >;

export interface CreateOrUpdateInstructionData {
  discriminator: number;
  createOrUpdateArgs: CreateOrUpdateArgs;
}

export interface CreateOrUpdateInstructionDataArgs {
  createOrUpdateArgs: CreateOrUpdateArgsArgs;
}

export function getCreateOrUpdateInstructionDataEncoder(): Encoder<CreateOrUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["createOrUpdateArgs", getCreateOrUpdateArgsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_OR_UPDATE_DISCRIMINATOR }),
  );
}

export function getCreateOrUpdateInstructionDataDecoder(): Decoder<CreateOrUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["createOrUpdateArgs", getCreateOrUpdateArgsDecoder()],
  ]);
}

export function getCreateOrUpdateInstructionDataCodec(): Codec<
  CreateOrUpdateInstructionDataArgs,
  CreateOrUpdateInstructionData
> {
  return combineCodec(
    getCreateOrUpdateInstructionDataEncoder(),
    getCreateOrUpdateInstructionDataDecoder(),
  );
}

export interface CreateOrUpdateInput<
  TAccountPayer extends string = string,
  TAccountRuleSetPda extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountBufferPda extends string = string,
> {
  /** Payer and creator of the RuleSet */
  payer: TransactionSigner<TAccountPayer>;
  /** The PDA account where the RuleSet is stored */
  ruleSetPda: Address<TAccountRuleSetPda>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** The buffer to copy a complete ruleset from */
  bufferPda?: Address<TAccountBufferPda>;
  createOrUpdateArgs: CreateOrUpdateInstructionDataArgs["createOrUpdateArgs"];
}

export function getCreateOrUpdateInstruction<
  TAccountPayer extends string,
  TAccountRuleSetPda extends string,
  TAccountSystemProgram extends string,
  TAccountBufferPda extends string,
  TProgramAddress extends Address = typeof MPL_TOKEN_AUTH_RULES_PROGRAM_ADDRESS,
>(
  input: CreateOrUpdateInput<
    TAccountPayer,
    TAccountRuleSetPda,
    TAccountSystemProgram,
    TAccountBufferPda
  >,
  config?: { programAddress?: TProgramAddress },
): CreateOrUpdateInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountRuleSetPda,
  TAccountSystemProgram,
  TAccountBufferPda
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MPL_TOKEN_AUTH_RULES_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    ruleSetPda: { value: input.ruleSetPda ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    bufferPda: { value: input.bufferPda ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("ruleSetPda", accounts.ruleSetPda),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("bufferPda", accounts.bufferPda),
    ],
    data: getCreateOrUpdateInstructionDataEncoder().encode(
      args as CreateOrUpdateInstructionDataArgs,
    ),
    programAddress,
  } as CreateOrUpdateInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountRuleSetPda,
    TAccountSystemProgram,
    TAccountBufferPda
  >);
}

export interface ParsedCreateOrUpdateInstruction<
  TProgram extends string = typeof MPL_TOKEN_AUTH_RULES_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    /** Payer and creator of the RuleSet */
    payer: TAccountMetas[0];
    /** The PDA account where the RuleSet is stored */
    ruleSetPda: TAccountMetas[1];
    /** System program */
    systemProgram: TAccountMetas[2];
    /** The buffer to copy a complete ruleset from */
    bufferPda?: TAccountMetas[3] | undefined;
  };
  data: CreateOrUpdateInstructionData;
}

export function parseCreateOrUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateOrUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === MPL_TOKEN_AUTH_RULES_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      ruleSetPda: getNextAccount(),
      systemProgram: getNextAccount(),
      bufferPda: getNextOptionalAccount(),
    },
    data: getCreateOrUpdateInstructionDataDecoder().decode(instruction.data),
  };
}
