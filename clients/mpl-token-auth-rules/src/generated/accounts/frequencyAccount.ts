/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  Account,
  Address,
  EncodedAccount,
  FetchAccountConfig,
  FetchAccountsConfig,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  MaybeAccount,
  MaybeEncodedAccount,
} from "@solana/kit";
import type { Key, KeyArgs } from "../types/index.js";
import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
} from "@solana/kit";
import { getKeyDecoder, getKeyEncoder } from "../types/index.js";

export interface FrequencyAccount {
  key: Key;
  lastUpdate: bigint;
  period: bigint;
}

export interface FrequencyAccountArgs {
  key: KeyArgs;
  lastUpdate: number | bigint;
  period: number | bigint;
}

/** Gets the encoder for {@link FrequencyAccountArgs} account data. */
export function getFrequencyAccountEncoder(): FixedSizeEncoder<FrequencyAccountArgs> {
  return getStructEncoder([
    ["key", getKeyEncoder()],
    ["lastUpdate", getI64Encoder()],
    ["period", getI64Encoder()],
  ]);
}

/** Gets the decoder for {@link FrequencyAccount} account data. */
export function getFrequencyAccountDecoder(): FixedSizeDecoder<FrequencyAccount> {
  return getStructDecoder([
    ["key", getKeyDecoder()],
    ["lastUpdate", getI64Decoder()],
    ["period", getI64Decoder()],
  ]);
}

/** Gets the codec for {@link FrequencyAccount} account data. */
export function getFrequencyAccountCodec(): FixedSizeCodec<
  FrequencyAccountArgs,
  FrequencyAccount
> {
  return combineCodec(
    getFrequencyAccountEncoder(),
    getFrequencyAccountDecoder(),
  );
}

export function decodeFrequencyAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<FrequencyAccount, TAddress>;
export function decodeFrequencyAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<FrequencyAccount, TAddress>;
export function decodeFrequencyAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<FrequencyAccount, TAddress>
  | MaybeAccount<FrequencyAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getFrequencyAccountDecoder(),
  );
}

export async function fetchFrequencyAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<FrequencyAccount, TAddress>> {
  const maybeAccount = await fetchMaybeFrequencyAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeFrequencyAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<FrequencyAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeFrequencyAccount(maybeAccount);
}

export async function fetchAllFrequencyAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Address[],
  config?: FetchAccountsConfig,
): Promise<Account<FrequencyAccount>[]> {
  const maybeAccounts = await fetchAllMaybeFrequencyAccount(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeFrequencyAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Address[],
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<FrequencyAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeFrequencyAccount(maybeAccount),
  );
}
