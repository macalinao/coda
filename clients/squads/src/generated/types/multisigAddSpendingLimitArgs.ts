/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
} from "@solana/kit";
import {
  getPeriodDecoder,
  getPeriodEncoder,
  type Period,
  type PeriodArgs,
} from "./index.js";

export type MultisigAddSpendingLimitArgs = {
  /** Key that is used to seed the SpendingLimit PDA. */
  createKey: Address;
  /** The index of the vault that the spending limit is for. */
  vaultIndex: number;
  /** The token mint the spending limit is for. */
  mint: Address;
  /**
   * The amount of tokens that can be spent in a period.
   * This amount is in decimals of the mint,
   * so 1 SOL would be `1_000_000_000` and 1 USDC would be `1_000_000`.
   */
  amount: bigint;
  /**
   * The reset period of the spending limit.
   * When it passes, the remaining amount is reset, unless it's `Period::OneTime`.
   */
  period: Period;
  /**
   * Members of the Spending Limit that can use it.
   * Don't have to be members of the multisig.
   */
  members: Array<Address>;
  /**
   * The destination addresses the spending limit is allowed to sent funds to.
   * If empty, funds can be sent to any address.
   */
  destinations: Array<Address>;
  /** Memo is used for indexing only. */
  memo: Option<string>;
};

export type MultisigAddSpendingLimitArgsArgs = {
  /** Key that is used to seed the SpendingLimit PDA. */
  createKey: Address;
  /** The index of the vault that the spending limit is for. */
  vaultIndex: number;
  /** The token mint the spending limit is for. */
  mint: Address;
  /**
   * The amount of tokens that can be spent in a period.
   * This amount is in decimals of the mint,
   * so 1 SOL would be `1_000_000_000` and 1 USDC would be `1_000_000`.
   */
  amount: number | bigint;
  /**
   * The reset period of the spending limit.
   * When it passes, the remaining amount is reset, unless it's `Period::OneTime`.
   */
  period: PeriodArgs;
  /**
   * Members of the Spending Limit that can use it.
   * Don't have to be members of the multisig.
   */
  members: Array<Address>;
  /**
   * The destination addresses the spending limit is allowed to sent funds to.
   * If empty, funds can be sent to any address.
   */
  destinations: Array<Address>;
  /** Memo is used for indexing only. */
  memo: OptionOrNullable<string>;
};

export function getMultisigAddSpendingLimitArgsEncoder(): Encoder<MultisigAddSpendingLimitArgsArgs> {
  return getStructEncoder([
    ["createKey", getAddressEncoder()],
    ["vaultIndex", getU8Encoder()],
    ["mint", getAddressEncoder()],
    ["amount", getU64Encoder()],
    ["period", getPeriodEncoder()],
    ["members", getArrayEncoder(getAddressEncoder())],
    ["destinations", getArrayEncoder(getAddressEncoder())],
    [
      "memo",
      getOptionEncoder(addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())),
    ],
  ]);
}

export function getMultisigAddSpendingLimitArgsDecoder(): Decoder<MultisigAddSpendingLimitArgs> {
  return getStructDecoder([
    ["createKey", getAddressDecoder()],
    ["vaultIndex", getU8Decoder()],
    ["mint", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["period", getPeriodDecoder()],
    ["members", getArrayDecoder(getAddressDecoder())],
    ["destinations", getArrayDecoder(getAddressDecoder())],
    [
      "memo",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getMultisigAddSpendingLimitArgsCodec(): Codec<
  MultisigAddSpendingLimitArgsArgs,
  MultisigAddSpendingLimitArgs
> {
  return combineCodec(
    getMultisigAddSpendingLimitArgsEncoder(),
    getMultisigAddSpendingLimitArgsDecoder(),
  );
}
