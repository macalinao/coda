/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
} from "@solana/kit";
import type { ResolvedAccount } from "../shared/index.js";
import type {
  GovernanceParameters,
  GovernanceParametersArgs,
} from "../types/index.js";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
} from "@solana/kit";
import { GOVERN_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
import {
  getGovernanceParametersDecoder,
  getGovernanceParametersEncoder,
} from "../types/index.js";

export const SET_GOVERNANCE_PARAMS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([175, 187, 3, 73, 8, 251, 67, 178]);

export function getSetGovernanceParamsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_GOVERNANCE_PARAMS_DISCRIMINATOR,
  );
}

export type SetGovernanceParamsInstruction<
  TProgram extends string = typeof GOVERN_PROGRAM_ADDRESS,
  TAccountGovernor extends string | AccountMeta = string,
  TAccountSmartWallet extends string | AccountMeta = string,
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGovernor extends string
        ? WritableAccount<TAccountGovernor>
        : TAccountGovernor,
      TAccountSmartWallet extends string
        ? ReadonlySignerAccount<TAccountSmartWallet> &
            AccountSignerMeta<TAccountSmartWallet>
        : TAccountSmartWallet,
      ...TRemainingAccounts,
    ]
  >;

export interface SetGovernanceParamsInstructionData {
  discriminator: ReadonlyUint8Array;
  params: GovernanceParameters;
}

export interface SetGovernanceParamsInstructionDataArgs {
  params: GovernanceParametersArgs;
}

export function getSetGovernanceParamsInstructionDataEncoder(): FixedSizeEncoder<SetGovernanceParamsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["params", getGovernanceParametersEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_GOVERNANCE_PARAMS_DISCRIMINATOR,
    }),
  );
}

export function getSetGovernanceParamsInstructionDataDecoder(): FixedSizeDecoder<SetGovernanceParamsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["params", getGovernanceParametersDecoder()],
  ]);
}

export function getSetGovernanceParamsInstructionDataCodec(): FixedSizeCodec<
  SetGovernanceParamsInstructionDataArgs,
  SetGovernanceParamsInstructionData
> {
  return combineCodec(
    getSetGovernanceParamsInstructionDataEncoder(),
    getSetGovernanceParamsInstructionDataDecoder(),
  );
}

export interface SetGovernanceParamsInput<
  TAccountGovernor extends string = string,
  TAccountSmartWallet extends string = string,
> {
  governor: Address<TAccountGovernor>;
  smartWallet: TransactionSigner<TAccountSmartWallet>;
  params: SetGovernanceParamsInstructionDataArgs["params"];
}

export function getSetGovernanceParamsInstruction<
  TAccountGovernor extends string,
  TAccountSmartWallet extends string,
  TProgramAddress extends Address = typeof GOVERN_PROGRAM_ADDRESS,
>(
  input: SetGovernanceParamsInput<TAccountGovernor, TAccountSmartWallet>,
  config?: { programAddress?: TProgramAddress },
): SetGovernanceParamsInstruction<
  TProgramAddress,
  TAccountGovernor,
  TAccountSmartWallet
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governor: { value: input.governor ?? null, isWritable: true },
    smartWallet: { value: input.smartWallet ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.governor),
      getAccountMeta(accounts.smartWallet),
    ],
    data: getSetGovernanceParamsInstructionDataEncoder().encode(
      args as SetGovernanceParamsInstructionDataArgs,
    ),
    programAddress,
  } as SetGovernanceParamsInstruction<
    TProgramAddress,
    TAccountGovernor,
    TAccountSmartWallet
  >);
}

export interface ParsedSetGovernanceParamsInstruction<
  TProgram extends string = typeof GOVERN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    governor: TAccountMetas[0];
    smartWallet: TAccountMetas[1];
  };
  data: SetGovernanceParamsInstructionData;
}

export function parseSetGovernanceParamsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetGovernanceParamsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { governor: getNextAccount(), smartWallet: getNextAccount() },
    data: getSetGovernanceParamsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
