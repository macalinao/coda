/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
  WritableSignerAccount,
} from "@solana/kit";
import type { ResolvedAccount } from "../shared/index.js";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
} from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";

export const EXIT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  234, 32, 12, 71, 126, 5, 219, 160,
]);

export function getExitDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXIT_DISCRIMINATOR);
}

export type ExitInstruction<
  TProgram extends string = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
  TAccountLocker extends string | AccountMeta = string,
  TAccountEscrow extends string | AccountMeta = string,
  TAccountEscrowOwner extends string | AccountMeta = string,
  TAccountEscrowTokens extends string | AccountMeta = string,
  TAccountDestinationTokens extends string | AccountMeta = string,
  TAccountPayer extends string | AccountMeta = string,
  TAccountTokenProgram extends
    | string
    | AccountMeta = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLocker extends string
        ? WritableAccount<TAccountLocker>
        : TAccountLocker,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountEscrowOwner extends string
        ? ReadonlySignerAccount<TAccountEscrowOwner> &
            AccountSignerMeta<TAccountEscrowOwner>
        : TAccountEscrowOwner,
      TAccountEscrowTokens extends string
        ? WritableAccount<TAccountEscrowTokens>
        : TAccountEscrowTokens,
      TAccountDestinationTokens extends string
        ? WritableAccount<TAccountDestinationTokens>
        : TAccountDestinationTokens,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export interface ExitInstructionData {
  discriminator: ReadonlyUint8Array;
}

export interface ExitInstructionDataArgs {}

export function getExitInstructionDataEncoder(): FixedSizeEncoder<ExitInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXIT_DISCRIMINATOR }),
  );
}

export function getExitInstructionDataDecoder(): FixedSizeDecoder<ExitInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExitInstructionDataCodec(): FixedSizeCodec<
  ExitInstructionDataArgs,
  ExitInstructionData
> {
  return combineCodec(
    getExitInstructionDataEncoder(),
    getExitInstructionDataDecoder(),
  );
}

export interface ExitInput<
  TAccountLocker extends string = string,
  TAccountEscrow extends string = string,
  TAccountEscrowOwner extends string = string,
  TAccountEscrowTokens extends string = string,
  TAccountDestinationTokens extends string = string,
  TAccountPayer extends string = string,
  TAccountTokenProgram extends string = string,
> {
  locker: Address<TAccountLocker>;
  escrow: Address<TAccountEscrow>;
  escrowOwner: TransactionSigner<TAccountEscrowOwner>;
  escrowTokens: Address<TAccountEscrowTokens>;
  destinationTokens: Address<TAccountDestinationTokens>;
  payer: TransactionSigner<TAccountPayer>;
  tokenProgram?: Address<TAccountTokenProgram>;
}

export function getExitInstruction<
  TAccountLocker extends string,
  TAccountEscrow extends string,
  TAccountEscrowOwner extends string,
  TAccountEscrowTokens extends string,
  TAccountDestinationTokens extends string,
  TAccountPayer extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
>(
  input: ExitInput<
    TAccountLocker,
    TAccountEscrow,
    TAccountEscrowOwner,
    TAccountEscrowTokens,
    TAccountDestinationTokens,
    TAccountPayer,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ExitInstruction<
  TProgramAddress,
  TAccountLocker,
  TAccountEscrow,
  TAccountEscrowOwner,
  TAccountEscrowTokens,
  TAccountDestinationTokens,
  TAccountPayer,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    locker: { value: input.locker ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    escrowOwner: { value: input.escrowOwner ?? null, isWritable: false },
    escrowTokens: { value: input.escrowTokens ?? null, isWritable: true },
    destinationTokens: {
      value: input.destinationTokens ?? null,
      isWritable: true,
    },
    payer: { value: input.payer ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.locker),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.escrowOwner),
      getAccountMeta(accounts.escrowTokens),
      getAccountMeta(accounts.destinationTokens),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getExitInstructionDataEncoder().encode({}),
    programAddress,
  } as ExitInstruction<
    TProgramAddress,
    TAccountLocker,
    TAccountEscrow,
    TAccountEscrowOwner,
    TAccountEscrowTokens,
    TAccountDestinationTokens,
    TAccountPayer,
    TAccountTokenProgram
  >);
}

export interface ParsedExitInstruction<
  TProgram extends string = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    locker: TAccountMetas[0];
    escrow: TAccountMetas[1];
    escrowOwner: TAccountMetas[2];
    escrowTokens: TAccountMetas[3];
    destinationTokens: TAccountMetas[4];
    payer: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: ExitInstructionData;
}

export function parseExitInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExitInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      locker: getNextAccount(),
      escrow: getNextAccount(),
      escrowOwner: getNextAccount(),
      escrowTokens: getNextAccount(),
      destinationTokens: getNextAccount(),
      payer: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getExitInstructionDataDecoder().decode(instruction.data),
  };
}
