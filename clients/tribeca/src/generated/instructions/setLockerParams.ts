/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
} from "@solana/kit";
import type { ResolvedAccount } from "../shared/index.js";
import type { LockerParams, LockerParamsArgs } from "../types/index.js";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
} from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
import {
  getLockerParamsDecoder,
  getLockerParamsEncoder,
} from "../types/index.js";

export const SET_LOCKER_PARAMS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([106, 39, 132, 84, 254, 77, 161, 169]);

export function getSetLockerParamsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_LOCKER_PARAMS_DISCRIMINATOR,
  );
}

export type SetLockerParamsInstruction<
  TProgram extends string = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
  TAccountLocker extends string | AccountMeta = string,
  TAccountGovernor extends string | AccountMeta = string,
  TAccountSmartWallet extends string | AccountMeta = string,
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLocker extends string
        ? WritableAccount<TAccountLocker>
        : TAccountLocker,
      TAccountGovernor extends string
        ? ReadonlyAccount<TAccountGovernor>
        : TAccountGovernor,
      TAccountSmartWallet extends string
        ? ReadonlySignerAccount<TAccountSmartWallet> &
            AccountSignerMeta<TAccountSmartWallet>
        : TAccountSmartWallet,
      ...TRemainingAccounts,
    ]
  >;

export interface SetLockerParamsInstructionData {
  discriminator: ReadonlyUint8Array;
  params: LockerParams;
}

export interface SetLockerParamsInstructionDataArgs {
  params: LockerParamsArgs;
}

export function getSetLockerParamsInstructionDataEncoder(): FixedSizeEncoder<SetLockerParamsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["params", getLockerParamsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_LOCKER_PARAMS_DISCRIMINATOR }),
  );
}

export function getSetLockerParamsInstructionDataDecoder(): FixedSizeDecoder<SetLockerParamsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["params", getLockerParamsDecoder()],
  ]);
}

export function getSetLockerParamsInstructionDataCodec(): FixedSizeCodec<
  SetLockerParamsInstructionDataArgs,
  SetLockerParamsInstructionData
> {
  return combineCodec(
    getSetLockerParamsInstructionDataEncoder(),
    getSetLockerParamsInstructionDataDecoder(),
  );
}

export interface SetLockerParamsInput<
  TAccountLocker extends string = string,
  TAccountGovernor extends string = string,
  TAccountSmartWallet extends string = string,
> {
  locker: Address<TAccountLocker>;
  governor: Address<TAccountGovernor>;
  smartWallet: TransactionSigner<TAccountSmartWallet>;
  params: SetLockerParamsInstructionDataArgs["params"];
}

export function getSetLockerParamsInstruction<
  TAccountLocker extends string,
  TAccountGovernor extends string,
  TAccountSmartWallet extends string,
  TProgramAddress extends Address = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
>(
  input: SetLockerParamsInput<
    TAccountLocker,
    TAccountGovernor,
    TAccountSmartWallet
  >,
  config?: { programAddress?: TProgramAddress },
): SetLockerParamsInstruction<
  TProgramAddress,
  TAccountLocker,
  TAccountGovernor,
  TAccountSmartWallet
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    locker: { value: input.locker ?? null, isWritable: true },
    governor: { value: input.governor ?? null, isWritable: false },
    smartWallet: { value: input.smartWallet ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.locker),
      getAccountMeta(accounts.governor),
      getAccountMeta(accounts.smartWallet),
    ],
    data: getSetLockerParamsInstructionDataEncoder().encode(
      args as SetLockerParamsInstructionDataArgs,
    ),
    programAddress,
  } as SetLockerParamsInstruction<
    TProgramAddress,
    TAccountLocker,
    TAccountGovernor,
    TAccountSmartWallet
  >);
}

export interface ParsedSetLockerParamsInstruction<
  TProgram extends string = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    locker: TAccountMetas[0];
    governor: TAccountMetas[1];
    smartWallet: TAccountMetas[2];
  };
  data: SetLockerParamsInstructionData;
}

export function parseSetLockerParamsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetLockerParamsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      locker: getNextAccount(),
      governor: getNextAccount(),
      smartWallet: getNextAccount(),
    },
    data: getSetLockerParamsInstructionDataDecoder().decode(instruction.data),
  };
}
