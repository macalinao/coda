/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
  WritableSignerAccount,
} from "@solana/kit";
import type { ResolvedAccount } from "../shared/index.js";
import type { LockerParams, LockerParamsArgs } from "../types/index.js";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
} from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
import {
  getLockerParamsDecoder,
  getLockerParamsEncoder,
} from "../types/index.js";

export const NEW_LOCKER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  177, 133, 32, 90, 229, 216, 131, 47,
]);

export function getNewLockerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(NEW_LOCKER_DISCRIMINATOR);
}

export type NewLockerInstruction<
  TProgram extends string = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
  TAccountBase extends string | AccountMeta = string,
  TAccountLocker extends string | AccountMeta = string,
  TAccountTokenMint extends string | AccountMeta = string,
  TAccountGovernor extends string | AccountMeta = string,
  TAccountPayer extends string | AccountMeta = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBase extends string
        ? ReadonlySignerAccount<TAccountBase> & AccountSignerMeta<TAccountBase>
        : TAccountBase,
      TAccountLocker extends string
        ? WritableAccount<TAccountLocker>
        : TAccountLocker,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountGovernor extends string
        ? ReadonlyAccount<TAccountGovernor>
        : TAccountGovernor,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export interface NewLockerInstructionData {
  discriminator: ReadonlyUint8Array;
  bump: number;
  params: LockerParams;
}

export interface NewLockerInstructionDataArgs {
  bump: number;
  params: LockerParamsArgs;
}

export function getNewLockerInstructionDataEncoder(): FixedSizeEncoder<NewLockerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
      ["params", getLockerParamsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: NEW_LOCKER_DISCRIMINATOR }),
  );
}

export function getNewLockerInstructionDataDecoder(): FixedSizeDecoder<NewLockerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
    ["params", getLockerParamsDecoder()],
  ]);
}

export function getNewLockerInstructionDataCodec(): FixedSizeCodec<
  NewLockerInstructionDataArgs,
  NewLockerInstructionData
> {
  return combineCodec(
    getNewLockerInstructionDataEncoder(),
    getNewLockerInstructionDataDecoder(),
  );
}

export interface NewLockerInput<
  TAccountBase extends string = string,
  TAccountLocker extends string = string,
  TAccountTokenMint extends string = string,
  TAccountGovernor extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> {
  base: TransactionSigner<TAccountBase>;
  locker: Address<TAccountLocker>;
  tokenMint: Address<TAccountTokenMint>;
  governor: Address<TAccountGovernor>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  bump: NewLockerInstructionDataArgs["bump"];
  params: NewLockerInstructionDataArgs["params"];
}

export function getNewLockerInstruction<
  TAccountBase extends string,
  TAccountLocker extends string,
  TAccountTokenMint extends string,
  TAccountGovernor extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
>(
  input: NewLockerInput<
    TAccountBase,
    TAccountLocker,
    TAccountTokenMint,
    TAccountGovernor,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): NewLockerInstruction<
  TProgramAddress,
  TAccountBase,
  TAccountLocker,
  TAccountTokenMint,
  TAccountGovernor,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    base: { value: input.base ?? null, isWritable: false },
    locker: { value: input.locker ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    governor: { value: input.governor ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.base),
      getAccountMeta(accounts.locker),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.governor),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getNewLockerInstructionDataEncoder().encode(
      args as NewLockerInstructionDataArgs,
    ),
    programAddress,
  } as NewLockerInstruction<
    TProgramAddress,
    TAccountBase,
    TAccountLocker,
    TAccountTokenMint,
    TAccountGovernor,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export interface ParsedNewLockerInstruction<
  TProgram extends string = typeof LOCKED_VOTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    base: TAccountMetas[0];
    locker: TAccountMetas[1];
    tokenMint: TAccountMetas[2];
    governor: TAccountMetas[3];
    payer: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: NewLockerInstructionData;
}

export function parseNewLockerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedNewLockerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      base: getNextAccount(),
      locker: getNextAccount(),
      tokenMint: getNextAccount(),
      governor: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getNewLockerInstructionDataDecoder().decode(instruction.data),
  };
}
