/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
} from "@solana/kit";
import type { ResolvedAccount } from "../shared/index.js";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
} from "@solana/kit";
import { SPL_GOVERNANCE_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";

export const COMPLETE_PROPOSAL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([3, 48, 26, 135, 144, 239, 5, 169]);

export function getCompleteProposalDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMPLETE_PROPOSAL_DISCRIMINATOR,
  );
}

export type CompleteProposalInstruction<
  TProgram extends string = typeof SPL_GOVERNANCE_PROGRAM_ADDRESS,
  TAccountProposalAccount extends string | AccountMeta = string,
  TAccountTokenOwnerRecord extends string | AccountMeta = string,
  TAccountCompleteProposalAuthority extends string | AccountMeta = string,
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProposalAccount extends string
        ? WritableAccount<TAccountProposalAccount>
        : TAccountProposalAccount,
      TAccountTokenOwnerRecord extends string
        ? ReadonlyAccount<TAccountTokenOwnerRecord>
        : TAccountTokenOwnerRecord,
      TAccountCompleteProposalAuthority extends string
        ? ReadonlySignerAccount<TAccountCompleteProposalAuthority> &
            AccountSignerMeta<TAccountCompleteProposalAuthority>
        : TAccountCompleteProposalAuthority,
      ...TRemainingAccounts,
    ]
  >;

export interface CompleteProposalInstructionData {
  discriminator: ReadonlyUint8Array;
}

export interface CompleteProposalInstructionDataArgs {}

export function getCompleteProposalInstructionDataEncoder(): FixedSizeEncoder<CompleteProposalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: COMPLETE_PROPOSAL_DISCRIMINATOR }),
  );
}

export function getCompleteProposalInstructionDataDecoder(): FixedSizeDecoder<CompleteProposalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCompleteProposalInstructionDataCodec(): FixedSizeCodec<
  CompleteProposalInstructionDataArgs,
  CompleteProposalInstructionData
> {
  return combineCodec(
    getCompleteProposalInstructionDataEncoder(),
    getCompleteProposalInstructionDataDecoder(),
  );
}

export interface CompleteProposalInput<
  TAccountProposalAccount extends string = string,
  TAccountTokenOwnerRecord extends string = string,
  TAccountCompleteProposalAuthority extends string = string,
> {
  proposalAccount: Address<TAccountProposalAccount>;
  /** TokenOwnerRecord account of the Proposal owner */
  tokenOwnerRecord: Address<TAccountTokenOwnerRecord>;
  /** Token Owner or Delegate */
  completeProposalAuthority: TransactionSigner<TAccountCompleteProposalAuthority>;
}

export function getCompleteProposalInstruction<
  TAccountProposalAccount extends string,
  TAccountTokenOwnerRecord extends string,
  TAccountCompleteProposalAuthority extends string,
  TProgramAddress extends Address = typeof SPL_GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: CompleteProposalInput<
    TAccountProposalAccount,
    TAccountTokenOwnerRecord,
    TAccountCompleteProposalAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): CompleteProposalInstruction<
  TProgramAddress,
  TAccountProposalAccount,
  TAccountTokenOwnerRecord,
  TAccountCompleteProposalAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPL_GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    proposalAccount: { value: input.proposalAccount ?? null, isWritable: true },
    tokenOwnerRecord: {
      value: input.tokenOwnerRecord ?? null,
      isWritable: false,
    },
    completeProposalAuthority: {
      value: input.completeProposalAuthority ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.proposalAccount),
      getAccountMeta(accounts.tokenOwnerRecord),
      getAccountMeta(accounts.completeProposalAuthority),
    ],
    data: getCompleteProposalInstructionDataEncoder().encode({}),
    programAddress,
  } as CompleteProposalInstruction<
    TProgramAddress,
    TAccountProposalAccount,
    TAccountTokenOwnerRecord,
    TAccountCompleteProposalAuthority
  >);
}

export interface ParsedCompleteProposalInstruction<
  TProgram extends string = typeof SPL_GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    proposalAccount: TAccountMetas[0];
    /** TokenOwnerRecord account of the Proposal owner */
    tokenOwnerRecord: TAccountMetas[1];
    /** Token Owner or Delegate */
    completeProposalAuthority: TAccountMetas[2];
  };
  data: CompleteProposalInstructionData;
}

export function parseCompleteProposalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCompleteProposalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      proposalAccount: getNextAccount(),
      tokenOwnerRecord: getNextAccount(),
      completeProposalAuthority: getNextAccount(),
    },
    data: getCompleteProposalInstructionDataDecoder().decode(instruction.data),
  };
}
