/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
} from "@solana/kit";
import {
  getUpdateAuthorityDecoder,
  getUpdateAuthorityEncoder,
  type UpdateAuthority,
  type UpdateAuthorityArgs,
} from "./index.js";

/** Arguments for `updateV1`. */
export type UpdateV1Args = {
  /** The asset's new display name, if changing. */
  newName: Option<string>;
  /** The asset's new metadata URI, if changing. */
  newUri: Option<string>;
  /** The asset's new update authority, if changing. */
  newUpdateAuthority: Option<UpdateAuthority>;
};

export type UpdateV1ArgsArgs = {
  /** The asset's new display name, if changing. */
  newName: OptionOrNullable<string>;
  /** The asset's new metadata URI, if changing. */
  newUri: OptionOrNullable<string>;
  /** The asset's new update authority, if changing. */
  newUpdateAuthority: OptionOrNullable<UpdateAuthorityArgs>;
};

export function getUpdateV1ArgsEncoder(): Encoder<UpdateV1ArgsArgs> {
  return getStructEncoder([
    [
      "newName",
      getOptionEncoder(addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())),
    ],
    [
      "newUri",
      getOptionEncoder(addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())),
    ],
    ["newUpdateAuthority", getOptionEncoder(getUpdateAuthorityEncoder())],
  ]);
}

export function getUpdateV1ArgsDecoder(): Decoder<UpdateV1Args> {
  return getStructDecoder([
    [
      "newName",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    [
      "newUri",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["newUpdateAuthority", getOptionDecoder(getUpdateAuthorityDecoder())],
  ]);
}

export function getUpdateV1ArgsCodec(): Codec<UpdateV1ArgsArgs, UpdateV1Args> {
  return combineCodec(getUpdateV1ArgsEncoder(), getUpdateV1ArgsDecoder());
}
