/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  type Codec,
  type Decoder,
  type Encoder,
  type Option,
  type OptionOrNullable,
} from "@solana/kit";
import {
  getCompressionProofDecoder,
  getCompressionProofEncoder,
  type CompressionProof,
  type CompressionProofArgs,
} from "./index.js";

/** Arguments for `transferV1`. */
export type TransferV1Args = {
  /**
   * Proof of the asset's current compressed state; required only
   * if the asset is compressed.
   */
  compressionProof: Option<CompressionProof>;
};

export type TransferV1ArgsArgs = {
  /**
   * Proof of the asset's current compressed state; required only
   * if the asset is compressed.
   */
  compressionProof: OptionOrNullable<CompressionProofArgs>;
};

export function getTransferV1ArgsEncoder(): Encoder<TransferV1ArgsArgs> {
  return getStructEncoder([
    ["compressionProof", getOptionEncoder(getCompressionProofEncoder())],
  ]);
}

export function getTransferV1ArgsDecoder(): Decoder<TransferV1Args> {
  return getStructDecoder([
    ["compressionProof", getOptionDecoder(getCompressionProofDecoder())],
  ]);
}

export function getTransferV1ArgsCodec(): Codec<
  TransferV1ArgsArgs,
  TransferV1Args
> {
  return combineCodec(getTransferV1ArgsEncoder(), getTransferV1ArgsDecoder());
}
