/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Codec,
  type Decoder,
  type Encoder,
} from "@solana/kit";
import {
  getRelationshipEntryDecoder,
  getRelationshipEntryEncoder,
  type RelationshipEntry,
  type RelationshipEntryArgs,
} from "./index.js";

/** Arguments for `createGroupV1`. */
export type CreateGroupV1Args = {
  /** The group's display name. */
  name: string;
  /** The URI pointing to the group's off-chain JSON metadata. */
  uri: string;
  /**
   * Initial member relationships (assets, collections or child
   * groups) to record on the group.
   */
  relationships: Array<RelationshipEntry>;
};

export type CreateGroupV1ArgsArgs = {
  /** The group's display name. */
  name: string;
  /** The URI pointing to the group's off-chain JSON metadata. */
  uri: string;
  /**
   * Initial member relationships (assets, collections or child
   * groups) to record on the group.
   */
  relationships: Array<RelationshipEntryArgs>;
};

export function getCreateGroupV1ArgsEncoder(): Encoder<CreateGroupV1ArgsArgs> {
  return getStructEncoder([
    ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ["uri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ["relationships", getArrayEncoder(getRelationshipEntryEncoder())],
  ]);
}

export function getCreateGroupV1ArgsDecoder(): Decoder<CreateGroupV1Args> {
  return getStructDecoder([
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["uri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["relationships", getArrayDecoder(getRelationshipEntryDecoder())],
  ]);
}

export function getCreateGroupV1ArgsCodec(): Codec<
  CreateGroupV1ArgsArgs,
  CreateGroupV1Args
> {
  return combineCodec(
    getCreateGroupV1ArgsEncoder(),
    getCreateGroupV1ArgsDecoder(),
  );
}
