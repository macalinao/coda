/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
  WritableSignerAccount,
} from "@solana/kit";
import type { ResolvedInstructionAccount } from "@solana/program-client-core";
import type { CloseGroupV1Args, CloseGroupV1ArgsArgs } from "../types/index.js";
import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
} from "@solana/kit";
import { getAccountMetaFactory } from "@solana/program-client-core";
import { MPL_CORE_PROGRAM_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  getCloseGroupV1ArgsDecoder,
  getCloseGroupV1ArgsEncoder,
} from "../types/index.js";

export const CLOSE_GROUP_V1_DISCRIMINATOR = 40;

export function getCloseGroupV1DiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CLOSE_GROUP_V1_DISCRIMINATOR);
}

export type CloseGroupV1Instruction<
  TProgram extends string = typeof MPL_CORE_PROGRAM_PROGRAM_ADDRESS,
  TAccountGroup extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGroup extends string
        ? WritableAccount<TAccountGroup>
        : TAccountGroup,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export interface CloseGroupV1InstructionData {
  discriminator: number;
  closeGroupV1Args: CloseGroupV1Args;
}

export interface CloseGroupV1InstructionDataArgs {
  closeGroupV1Args: CloseGroupV1ArgsArgs;
}

export function getCloseGroupV1InstructionDataEncoder(): FixedSizeEncoder<CloseGroupV1InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["closeGroupV1Args", getCloseGroupV1ArgsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_GROUP_V1_DISCRIMINATOR }),
  );
}

export function getCloseGroupV1InstructionDataDecoder(): FixedSizeDecoder<CloseGroupV1InstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["closeGroupV1Args", getCloseGroupV1ArgsDecoder()],
  ]);
}

export function getCloseGroupV1InstructionDataCodec(): FixedSizeCodec<
  CloseGroupV1InstructionDataArgs,
  CloseGroupV1InstructionData
> {
  return combineCodec(
    getCloseGroupV1InstructionDataEncoder(),
    getCloseGroupV1InstructionDataDecoder(),
  );
}

export interface CloseGroupV1Input<
  TAccountGroup extends string = string,
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
> {
  /** The address of the group to close */
  group: Address<TAccountGroup>;
  /** The account receiving reclaimed lamports */
  payer: TransactionSigner<TAccountPayer>;
  /** The update authority of the group */
  authority?: TransactionSigner<TAccountAuthority>;
  closeGroupV1Args: CloseGroupV1InstructionDataArgs["closeGroupV1Args"];
}

export function getCloseGroupV1Instruction<
  TAccountGroup extends string,
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof MPL_CORE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CloseGroupV1Input<TAccountGroup, TAccountPayer, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): CloseGroupV1Instruction<
  TProgramAddress,
  TAccountGroup,
  TAccountPayer,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MPL_CORE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    group: { value: input.group ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("group", accounts.group),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getCloseGroupV1InstructionDataEncoder().encode(
      args as CloseGroupV1InstructionDataArgs,
    ),
    programAddress,
  } as CloseGroupV1Instruction<
    TProgramAddress,
    TAccountGroup,
    TAccountPayer,
    TAccountAuthority
  >);
}

export interface ParsedCloseGroupV1Instruction<
  TProgram extends string = typeof MPL_CORE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the group to close */
    group: TAccountMetas[0];
    /** The account receiving reclaimed lamports */
    payer: TAccountMetas[1];
    /** The update authority of the group */
    authority?: TAccountMetas[2] | undefined;
  };
  data: CloseGroupV1InstructionData;
}

export function parseCloseGroupV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseGroupV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === MPL_CORE_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      group: getNextAccount(),
      payer: getNextAccount(),
      authority: getNextOptionalAccount(),
    },
    data: getCloseGroupV1InstructionDataDecoder().decode(instruction.data),
  };
}
