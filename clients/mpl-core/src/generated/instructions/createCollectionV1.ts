/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { MPL_CORE_PROGRAM_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  getCreateCollectionV1ArgsDecoder,
  getCreateCollectionV1ArgsEncoder,
  type CreateCollectionV1Args,
  type CreateCollectionV1ArgsArgs,
} from "../types/index.js";

export const CREATE_COLLECTION_V1_DISCRIMINATOR = 1;

export function getCreateCollectionV1DiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CREATE_COLLECTION_V1_DISCRIMINATOR);
}

export type CreateCollectionV1Instruction<
  TProgram extends string = typeof MPL_CORE_PROGRAM_PROGRAM_ADDRESS,
  TAccountCollection extends string | AccountMeta<string> = string,
  TAccountUpdateAuthority extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCollection extends string
        ? WritableSignerAccount<TAccountCollection> &
            AccountSignerMeta<TAccountCollection>
        : TAccountCollection,
      TAccountUpdateAuthority extends string
        ? ReadonlyAccount<TAccountUpdateAuthority>
        : TAccountUpdateAuthority,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateCollectionV1InstructionData = {
  discriminator: number;
  /** The collection's name, URI and initial plugins. */
  createCollectionV1Args: CreateCollectionV1Args;
};

export type CreateCollectionV1InstructionDataArgs = {
  /** The collection's name, URI and initial plugins. */
  createCollectionV1Args: CreateCollectionV1ArgsArgs;
};

export function getCreateCollectionV1InstructionDataEncoder(): Encoder<CreateCollectionV1InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["createCollectionV1Args", getCreateCollectionV1ArgsEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATE_COLLECTION_V1_DISCRIMINATOR,
    }),
  );
}

export function getCreateCollectionV1InstructionDataDecoder(): Decoder<CreateCollectionV1InstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["createCollectionV1Args", getCreateCollectionV1ArgsDecoder()],
  ]);
}

export function getCreateCollectionV1InstructionDataCodec(): Codec<
  CreateCollectionV1InstructionDataArgs,
  CreateCollectionV1InstructionData
> {
  return combineCodec(
    getCreateCollectionV1InstructionDataEncoder(),
    getCreateCollectionV1InstructionDataDecoder(),
  );
}

export type CreateCollectionV1Input<
  TAccountCollection extends string = string,
  TAccountUpdateAuthority extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The address of the new asset */
  collection: TransactionSigner<TAccountCollection>;
  /** The authority of the new asset */
  updateAuthority?: Address<TAccountUpdateAuthority>;
  /** The account paying for the storage fees */
  payer: TransactionSigner<TAccountPayer>;
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>;
  createCollectionV1Args: CreateCollectionV1InstructionDataArgs["createCollectionV1Args"];
};

/**
 * Creates a new Core collection — a single account that groups
 * assets under a shared update authority and collection-level
 * plugins (e.g. shared royalties).
 *
 * Like assets, collections are keypair-generated accounts rather
 * than PDAs.
 */
export function getCreateCollectionV1Instruction<
  TAccountCollection extends string,
  TAccountUpdateAuthority extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MPL_CORE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CreateCollectionV1Input<
    TAccountCollection,
    TAccountUpdateAuthority,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateCollectionV1Instruction<
  TProgramAddress,
  TAccountCollection,
  TAccountUpdateAuthority,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MPL_CORE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    collection: { value: input.collection ?? null, isWritable: true },
    updateAuthority: {
      value: input.updateAuthority ?? null,
      isWritable: false,
    },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("collection", accounts.collection),
      getAccountMeta("updateAuthority", accounts.updateAuthority),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateCollectionV1InstructionDataEncoder().encode(
      args as CreateCollectionV1InstructionDataArgs,
    ),
    programAddress,
  } as CreateCollectionV1Instruction<
    TProgramAddress,
    TAccountCollection,
    TAccountUpdateAuthority,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedCreateCollectionV1Instruction<
  TProgram extends string = typeof MPL_CORE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The address of the new asset */
    collection: TAccountMetas[0];
    /** The authority of the new asset */
    updateAuthority?: TAccountMetas[1] | undefined;
    /** The account paying for the storage fees */
    payer: TAccountMetas[2];
    /** The system program */
    systemProgram: TAccountMetas[3];
  };
  data: CreateCollectionV1InstructionData;
};

export function parseCreateCollectionV1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateCollectionV1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === MPL_CORE_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      collection: getNextAccount(),
      updateAuthority: getNextOptionalAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateCollectionV1InstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
