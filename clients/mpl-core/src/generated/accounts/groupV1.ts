/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
} from "@solana/kit";
import {
  getKeyDecoder,
  getKeyEncoder,
  type Key,
  type KeyArgs,
} from "../types/index.js";

/**
 * An MPL Core group: a single account organizing assets,
 * collections and other groups under a shared update authority,
 * independent of the collection hierarchy.
 */
export type GroupV1 = {
  /** Account discriminator; always `Key.GroupV1`. */
  key: Key;
  /** The authority allowed to update the group and manage its membership. */
  updateAuthority: Address;
  /** The group's display name. */
  name: string;
  /** The URI pointing to the group's off-chain JSON metadata. */
  uri: string;
  /** The addresses of the collections that are direct members of this group. */
  collections: Array<Address>;
  /** The addresses of the child groups nested under this group. */
  groups: Array<Address>;
  /** The addresses of the groups that this group is itself a member of. */
  parentGroups: Array<Address>;
  /** The addresses of the assets that are direct members of this group. */
  assets: Array<Address>;
};

export type GroupV1Args = {
  /** Account discriminator; always `Key.GroupV1`. */
  key: KeyArgs;
  /** The authority allowed to update the group and manage its membership. */
  updateAuthority: Address;
  /** The group's display name. */
  name: string;
  /** The URI pointing to the group's off-chain JSON metadata. */
  uri: string;
  /** The addresses of the collections that are direct members of this group. */
  collections: Array<Address>;
  /** The addresses of the child groups nested under this group. */
  groups: Array<Address>;
  /** The addresses of the groups that this group is itself a member of. */
  parentGroups: Array<Address>;
  /** The addresses of the assets that are direct members of this group. */
  assets: Array<Address>;
};

/** Gets the encoder for {@link GroupV1Args} account data. */
export function getGroupV1Encoder(): Encoder<GroupV1Args> {
  return getStructEncoder([
    ["key", getKeyEncoder()],
    ["updateAuthority", getAddressEncoder()],
    ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ["uri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ["collections", getArrayEncoder(getAddressEncoder())],
    ["groups", getArrayEncoder(getAddressEncoder())],
    ["parentGroups", getArrayEncoder(getAddressEncoder())],
    ["assets", getArrayEncoder(getAddressEncoder())],
  ]);
}

/** Gets the decoder for {@link GroupV1} account data. */
export function getGroupV1Decoder(): Decoder<GroupV1> {
  return getStructDecoder([
    ["key", getKeyDecoder()],
    ["updateAuthority", getAddressDecoder()],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["uri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["collections", getArrayDecoder(getAddressDecoder())],
    ["groups", getArrayDecoder(getAddressDecoder())],
    ["parentGroups", getArrayDecoder(getAddressDecoder())],
    ["assets", getArrayDecoder(getAddressDecoder())],
  ]);
}

/** Gets the codec for {@link GroupV1} account data. */
export function getGroupV1Codec(): Codec<GroupV1Args, GroupV1> {
  return combineCodec(getGroupV1Encoder(), getGroupV1Decoder());
}

export function decodeGroupV1<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GroupV1, TAddress>;
export function decodeGroupV1<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GroupV1, TAddress>;
export function decodeGroupV1<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GroupV1, TAddress> | MaybeAccount<GroupV1, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGroupV1Decoder(),
  );
}

export async function fetchGroupV1<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GroupV1, TAddress>> {
  const maybeAccount = await fetchMaybeGroupV1(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGroupV1<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GroupV1, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGroupV1(maybeAccount);
}

export async function fetchAllGroupV1(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GroupV1>[]> {
  const maybeAccounts = await fetchAllMaybeGroupV1(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGroupV1(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GroupV1>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGroupV1(maybeAccount));
}
