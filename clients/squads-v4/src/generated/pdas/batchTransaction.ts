/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  getAddressEncoder,
  getProgramDerivedAddress,
  getU32Encoder,
  getU64Encoder,
  getUtf8Encoder,
  type Address,
  type ProgramDerivedAddress,
} from "@solana/kit";

export type BatchTransactionSeeds = {
  /** The multisig that owns the batch */
  multisig: Address;
  /** The transaction index of the batch */
  batchIndex: number | bigint;
  /** The position within the batch, starting at 1 */
  transactionIndex: number;
};

/**
 * An individual transaction inside a batch. `batchIndex` is the
 * transaction index of the batch itself; `transactionIndex` is the
 * position within that batch, starting at 1.
 */
export async function findBatchTransactionPda(
  seeds: BatchTransactionSeeds,
  config: { programAddress?: Address | undefined } = {},
): Promise<ProgramDerivedAddress> {
  const {
    programAddress = "SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf" as Address<"SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf">,
  } = config;
  return await getProgramDerivedAddress({
    programAddress,
    seeds: [
      getUtf8Encoder().encode("multisig"),
      getAddressEncoder().encode(seeds.multisig),
      getUtf8Encoder().encode("transaction"),
      getU64Encoder().encode(seeds.batchIndex),
      getUtf8Encoder().encode("batch_transaction"),
      getU32Encoder().encode(seeds.transactionIndex),
    ],
  });
}
