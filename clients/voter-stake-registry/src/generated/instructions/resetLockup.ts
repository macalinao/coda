/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlyAccount,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
} from "@solana/kit";
import type { ResolvedInstructionAccount } from "@solana/program-client-core";
import type { LockupKind, LockupKindArgs } from "../types/index.js";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
} from "@solana/kit";
import { getAccountMetaFactory } from "@solana/program-client-core";
import { VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS } from "../programs/index.js";
import { getLockupKindDecoder, getLockupKindEncoder } from "../types/index.js";

export const RESET_LOCKUP_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  243, 20, 24, 247, 238, 148, 94, 62,
]);

export function getResetLockupDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESET_LOCKUP_DISCRIMINATOR,
  );
}

export type ResetLockupInstruction<
  TProgram extends string = typeof VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS,
  TAccountRegistrar extends string | AccountMeta = string,
  TAccountVoter extends string | AccountMeta = string,
  TAccountVoterAuthority extends string | AccountMeta = string,
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRegistrar extends string
        ? ReadonlyAccount<TAccountRegistrar>
        : TAccountRegistrar,
      TAccountVoter extends string
        ? WritableAccount<TAccountVoter>
        : TAccountVoter,
      TAccountVoterAuthority extends string
        ? ReadonlySignerAccount<TAccountVoterAuthority> &
            AccountSignerMeta<TAccountVoterAuthority>
        : TAccountVoterAuthority,
      ...TRemainingAccounts,
    ]
  >;

export interface ResetLockupInstructionData {
  discriminator: ReadonlyUint8Array;
  depositEntryIndex: number;
  kind: LockupKind;
  periods: number;
}

export interface ResetLockupInstructionDataArgs {
  depositEntryIndex: number;
  kind: LockupKindArgs;
  periods: number;
}

export function getResetLockupInstructionDataEncoder(): FixedSizeEncoder<ResetLockupInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["depositEntryIndex", getU8Encoder()],
      ["kind", getLockupKindEncoder()],
      ["periods", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RESET_LOCKUP_DISCRIMINATOR }),
  );
}

export function getResetLockupInstructionDataDecoder(): FixedSizeDecoder<ResetLockupInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["depositEntryIndex", getU8Decoder()],
    ["kind", getLockupKindDecoder()],
    ["periods", getU32Decoder()],
  ]);
}

export function getResetLockupInstructionDataCodec(): FixedSizeCodec<
  ResetLockupInstructionDataArgs,
  ResetLockupInstructionData
> {
  return combineCodec(
    getResetLockupInstructionDataEncoder(),
    getResetLockupInstructionDataDecoder(),
  );
}

export interface ResetLockupInput<
  TAccountRegistrar extends string = string,
  TAccountVoter extends string = string,
  TAccountVoterAuthority extends string = string,
> {
  registrar: Address<TAccountRegistrar>;
  voter: Address<TAccountVoter>;
  voterAuthority: TransactionSigner<TAccountVoterAuthority>;
  depositEntryIndex: ResetLockupInstructionDataArgs["depositEntryIndex"];
  kind: ResetLockupInstructionDataArgs["kind"];
  periods: ResetLockupInstructionDataArgs["periods"];
}

export function getResetLockupInstruction<
  TAccountRegistrar extends string,
  TAccountVoter extends string,
  TAccountVoterAuthority extends string,
  TProgramAddress extends Address = typeof VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ResetLockupInput<
    TAccountRegistrar,
    TAccountVoter,
    TAccountVoterAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): ResetLockupInstruction<
  TProgramAddress,
  TAccountRegistrar,
  TAccountVoter,
  TAccountVoterAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    registrar: { value: input.registrar ?? null, isWritable: false },
    voter: { value: input.voter ?? null, isWritable: true },
    voterAuthority: { value: input.voterAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("registrar", accounts.registrar),
      getAccountMeta("voter", accounts.voter),
      getAccountMeta("voterAuthority", accounts.voterAuthority),
    ],
    data: getResetLockupInstructionDataEncoder().encode(
      args as ResetLockupInstructionDataArgs,
    ),
    programAddress,
  } as ResetLockupInstruction<
    TProgramAddress,
    TAccountRegistrar,
    TAccountVoter,
    TAccountVoterAuthority
  >);
}

export interface ParsedResetLockupInstruction<
  TProgram extends string = typeof VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    registrar: TAccountMetas[0];
    voter: TAccountMetas[1];
    voterAuthority: TAccountMetas[2];
  };
  data: ResetLockupInstructionData;
}

export function parseResetLockupInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResetLockupInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      registrar: getNextAccount(),
      voter: getNextAccount(),
      voterAuthority: getNextAccount(),
    },
    data: getResetLockupInstructionDataDecoder().decode(instruction.data),
  };
}
