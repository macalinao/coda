/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type {
  AccountMeta,
  AccountSignerMeta,
  Address,
  FixedSizeCodec,
  FixedSizeDecoder,
  FixedSizeEncoder,
  Instruction,
  InstructionWithAccounts,
  InstructionWithData,
  ReadonlySignerAccount,
  ReadonlyUint8Array,
  TransactionSigner,
  WritableAccount,
} from "@solana/kit";
import type { ResolvedAccount } from "../shared/index.js";
import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
} from "@solana/kit";
import { VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";

export const CLOSE_DEPOSIT_ENTRY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([236, 190, 87, 34, 251, 131, 138, 237]);

export function getCloseDepositEntryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_DEPOSIT_ENTRY_DISCRIMINATOR,
  );
}

export type CloseDepositEntryInstruction<
  TProgram extends string = typeof VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS,
  TAccountVoter extends string | AccountMeta = string,
  TAccountVoterAuthority extends string | AccountMeta = string,
  TRemainingAccounts extends readonly AccountMeta[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVoter extends string
        ? WritableAccount<TAccountVoter>
        : TAccountVoter,
      TAccountVoterAuthority extends string
        ? ReadonlySignerAccount<TAccountVoterAuthority> &
            AccountSignerMeta<TAccountVoterAuthority>
        : TAccountVoterAuthority,
      ...TRemainingAccounts,
    ]
  >;

export interface CloseDepositEntryInstructionData {
  discriminator: ReadonlyUint8Array;
  depositEntryIndex: number;
}

export interface CloseDepositEntryInstructionDataArgs {
  depositEntryIndex: number;
}

export function getCloseDepositEntryInstructionDataEncoder(): FixedSizeEncoder<CloseDepositEntryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["depositEntryIndex", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_DEPOSIT_ENTRY_DISCRIMINATOR }),
  );
}

export function getCloseDepositEntryInstructionDataDecoder(): FixedSizeDecoder<CloseDepositEntryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["depositEntryIndex", getU8Decoder()],
  ]);
}

export function getCloseDepositEntryInstructionDataCodec(): FixedSizeCodec<
  CloseDepositEntryInstructionDataArgs,
  CloseDepositEntryInstructionData
> {
  return combineCodec(
    getCloseDepositEntryInstructionDataEncoder(),
    getCloseDepositEntryInstructionDataDecoder(),
  );
}

export interface CloseDepositEntryInput<
  TAccountVoter extends string = string,
  TAccountVoterAuthority extends string = string,
> {
  voter: Address<TAccountVoter>;
  voterAuthority: TransactionSigner<TAccountVoterAuthority>;
  depositEntryIndex: CloseDepositEntryInstructionDataArgs["depositEntryIndex"];
}

export function getCloseDepositEntryInstruction<
  TAccountVoter extends string,
  TAccountVoterAuthority extends string,
  TProgramAddress extends Address = typeof VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CloseDepositEntryInput<TAccountVoter, TAccountVoterAuthority>,
  config?: { programAddress?: TProgramAddress },
): CloseDepositEntryInstruction<
  TProgramAddress,
  TAccountVoter,
  TAccountVoterAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    voter: { value: input.voter ?? null, isWritable: true },
    voterAuthority: { value: input.voterAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.voter),
      getAccountMeta(accounts.voterAuthority),
    ],
    data: getCloseDepositEntryInstructionDataEncoder().encode(
      args as CloseDepositEntryInstructionDataArgs,
    ),
    programAddress,
  } as CloseDepositEntryInstruction<
    TProgramAddress,
    TAccountVoter,
    TAccountVoterAuthority
  >);
}

export interface ParsedCloseDepositEntryInstruction<
  TProgram extends string = typeof VOTER_STAKE_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> {
  programAddress: Address<TProgram>;
  accounts: {
    voter: TAccountMetas[0];
    voterAuthority: TAccountMetas[1];
  };
  data: CloseDepositEntryInstructionData;
}

export function parseCloseDepositEntryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseDepositEntryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { voter: getNextAccount(), voterAuthority: getNextAccount() },
    data: getCloseDepositEntryInstructionDataDecoder().decode(instruction.data),
  };
}
