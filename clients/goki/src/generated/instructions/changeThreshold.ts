/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SMART_WALLET_PROGRAM_ADDRESS } from "../programs/index.js";

export const CHANGE_THRESHOLD_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([146, 151, 213, 63, 121, 79, 9, 29]);

export function getChangeThresholdDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHANGE_THRESHOLD_DISCRIMINATOR,
  );
}

export type ChangeThresholdInstruction<
  TProgram extends string = typeof SMART_WALLET_PROGRAM_ADDRESS,
  TAccountSmartWallet extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSmartWallet extends string
        ? WritableSignerAccount<TAccountSmartWallet> &
            AccountSignerMeta<TAccountSmartWallet>
        : TAccountSmartWallet,
      ...TRemainingAccounts,
    ]
  >;

export type ChangeThresholdInstructionData = {
  discriminator: ReadonlyUint8Array;
  threshold: bigint;
};

export type ChangeThresholdInstructionDataArgs = { threshold: number | bigint };

export function getChangeThresholdInstructionDataEncoder(): FixedSizeEncoder<ChangeThresholdInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["threshold", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CHANGE_THRESHOLD_DISCRIMINATOR }),
  );
}

export function getChangeThresholdInstructionDataDecoder(): FixedSizeDecoder<ChangeThresholdInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["threshold", getU64Decoder()],
  ]);
}

export function getChangeThresholdInstructionDataCodec(): FixedSizeCodec<
  ChangeThresholdInstructionDataArgs,
  ChangeThresholdInstructionData
> {
  return combineCodec(
    getChangeThresholdInstructionDataEncoder(),
    getChangeThresholdInstructionDataDecoder(),
  );
}

export type ChangeThresholdInput<TAccountSmartWallet extends string = string> =
  {
    smartWallet: TransactionSigner<TAccountSmartWallet>;
    threshold: ChangeThresholdInstructionDataArgs["threshold"];
  };

export function getChangeThresholdInstruction<
  TAccountSmartWallet extends string,
  TProgramAddress extends Address = typeof SMART_WALLET_PROGRAM_ADDRESS,
>(
  input: ChangeThresholdInput<TAccountSmartWallet>,
  config?: { programAddress?: TProgramAddress },
): ChangeThresholdInstruction<TProgramAddress, TAccountSmartWallet> {
  // Program address.
  const programAddress = config?.programAddress ?? SMART_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    smartWallet: { value: input.smartWallet ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("smartWallet", accounts.smartWallet)],
    data: getChangeThresholdInstructionDataEncoder().encode(
      args as ChangeThresholdInstructionDataArgs,
    ),
    programAddress,
  } as ChangeThresholdInstruction<TProgramAddress, TAccountSmartWallet>);
}

export type ParsedChangeThresholdInstruction<
  TProgram extends string = typeof SMART_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    smartWallet: TAccountMetas[0];
  };
  data: ChangeThresholdInstructionData;
};

export function parseChangeThresholdInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedChangeThresholdInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { smartWallet: getNextAccount() },
    data: getChangeThresholdInstructionDataDecoder().decode(instruction.data),
  };
}
