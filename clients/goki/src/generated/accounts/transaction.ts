/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { findTransactionPda, type TransactionSeeds } from "../pdas/index.js";
import {
  getTXInstructionDecoder,
  getTXInstructionEncoder,
  type TXInstruction,
  type TXInstructionArgs,
} from "../types/index.js";

export const TRANSACTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  11, 24, 174, 129, 203, 117, 242, 23,
]);

export function getTransactionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TRANSACTION_DISCRIMINATOR);
}

export type Transaction = {
  discriminator: ReadonlyUint8Array;
  smartWallet: Address;
  index: bigint;
  bump: number;
  proposer: Address;
  instructions: Array<TXInstruction>;
  signers: Array<boolean>;
  ownerSetSeqno: number;
  eta: bigint;
  executor: Address;
  executedAt: bigint;
};

export type TransactionArgs = {
  smartWallet: Address;
  index: number | bigint;
  bump: number;
  proposer: Address;
  instructions: Array<TXInstructionArgs>;
  signers: Array<boolean>;
  ownerSetSeqno: number;
  eta: number | bigint;
  executor: Address;
  executedAt: number | bigint;
};

/** Gets the encoder for {@link TransactionArgs} account data. */
export function getTransactionEncoder(): Encoder<TransactionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["smartWallet", getAddressEncoder()],
      ["index", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["proposer", getAddressEncoder()],
      ["instructions", getArrayEncoder(getTXInstructionEncoder())],
      ["signers", getArrayEncoder(getBooleanEncoder())],
      ["ownerSetSeqno", getU32Encoder()],
      ["eta", getI64Encoder()],
      ["executor", getAddressEncoder()],
      ["executedAt", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TRANSACTION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Transaction} account data. */
export function getTransactionDecoder(): Decoder<Transaction> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["smartWallet", getAddressDecoder()],
    ["index", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["proposer", getAddressDecoder()],
    ["instructions", getArrayDecoder(getTXInstructionDecoder())],
    ["signers", getArrayDecoder(getBooleanDecoder())],
    ["ownerSetSeqno", getU32Decoder()],
    ["eta", getI64Decoder()],
    ["executor", getAddressDecoder()],
    ["executedAt", getI64Decoder()],
  ]);
}

/** Gets the codec for {@link Transaction} account data. */
export function getTransactionCodec(): Codec<TransactionArgs, Transaction> {
  return combineCodec(getTransactionEncoder(), getTransactionDecoder());
}

export function decodeTransaction<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Transaction, TAddress>;
export function decodeTransaction<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Transaction, TAddress>;
export function decodeTransaction<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Transaction, TAddress> | MaybeAccount<Transaction, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTransactionDecoder(),
  );
}

export async function fetchTransaction<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Transaction, TAddress>> {
  const maybeAccount = await fetchMaybeTransaction(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTransaction<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Transaction, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTransaction(maybeAccount);
}

export async function fetchAllTransaction(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Transaction>[]> {
  const maybeAccounts = await fetchAllMaybeTransaction(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTransaction(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Transaction>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTransaction(maybeAccount));
}

export async function fetchTransactionFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: TransactionSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<Transaction>> {
  const maybeAccount = await fetchMaybeTransactionFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTransactionFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: TransactionSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<Transaction>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findTransactionPda(seeds, { programAddress });
  return await fetchMaybeTransaction(rpc, address, fetchConfig);
}
